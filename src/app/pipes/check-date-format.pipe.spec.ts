import { CheckDateFormatPipe } from './check-date-format.pipe';

describe('CheckDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
